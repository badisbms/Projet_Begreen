{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<style>
		.btn[type="submit"] {
			margin: 2em 0 1.5em;
			letter-spacing: 3px;
			color: white;
			background-color: #5cb136;
		}
		.email {}

		.logbox {
			border: solid 2px;
			height: 80%;
			width: 40%;
			position: absolut;
			margin-left: 34%;
			margin-top: 10%;
			margin-right: 30%;
			border-radius: 10%;
			{
				# background: linear-gradient(90deg, #6e865e, #5cb136); #
			}
		}

		body {
			background: color: #5cb136;
			background: linear-gradient(90deg, #5cb136, #55e7fc);
		}


		.button {
			background-color: teal;
			color: white;
			font-weight: bold;
			border-radius: 3px;
			border-color: rgb(0, 128, 128);
			padding: 2%;
			border-style: solid;
			margin-left: 110px;

		}
		label.required {
			display: none;
		}
		.bg {
			background-color: red;
			padding: 20px;
			width: 402px;
			height: 500px;
			margin-top: 77px;
			border-radius: 10px;

		}
	</style>

	{% for flashError in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}

	<h1 class="text-center">Register</h1>
	<div class="bg container-sm">
		<div class="text-center container-sm">
			{{ form_start(registrationForm  )}}
			{{ form_row(registrationForm.name, { 'attr': {'class': 'name', 'placeholder': 'Name'} }) }}
			{{ form_row(registrationForm.lastName, { 'attr': {'class': 'lastName', 'placeholder': 'Last name'} }) }}

			{{ form_row(registrationForm.email,{ 'attr': {'class': 'email', 'placeholder': 'Email'} }) }}
			{{ form_row(registrationForm.plainPassword, { 'attr': {'class': 'Password','placeholder': 'Password,'} })}}
			{# label: 'Password' #}
			{# }} #}
				<button type="submit" class="btn"> Register</button>
			{{ form_end(registrationForm) }}

		</div>
	</div>


{% endblock %}
